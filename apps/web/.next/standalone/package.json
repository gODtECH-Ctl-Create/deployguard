{
  "name": "deployguard",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "concurrently \"npm run dev --workspace=@deployguard/api\" \"npm run dev --workspace=@deployguard/web\"",
    "build": "npm run build --workspace=@deployguard/api && npm run build --workspace=@deployguard/web",
    "typecheck": "npm run typecheck --workspace=@deployguard/api && npm run typecheck --workspace=@deployguard/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}