apiVersion: apps/v1
kind: Deployment
metadata:
  name: deployguard-web
  namespace: deployguard
  labels:
    app.kubernetes.io/name: deployguard
    app.kubernetes.io/component: web
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: deployguard
      app.kubernetes.io/component: web
  template:
    metadata:
      labels:
        app.kubernetes.io/name: deployguard
        app.kubernetes.io/component: web
    spec:
      containers:
        - name: web
          image: deployguard-web:local
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 3000
          env:
            - name: PORT
              valueFrom:
                configMapKeyRef:
                  name: deployguard-config
                  key: WEB_PORT
            - name: API_URL
              valueFrom:
                configMapKeyRef:
                  name: deployguard-config
                  key: API_URL
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 10
            periodSeconds: 10
            timeoutSeconds: 3
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 30
            periodSeconds: 20
            timeoutSeconds: 3
            failureThreshold: 3
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              memory: 512Mi
---
apiVersion: v1
kind: Service
metadata:
  name: deployguard-web
  namespace: deployguard
  labels:
    app.kubernetes.io/name: deployguard
    app.kubernetes.io/component: web
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: deployguard
    app.kubernetes.io/component: web
  ports:
    - name: http
      port: 3000
      targetPort: http
